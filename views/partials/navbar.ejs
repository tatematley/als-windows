<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Al's Windows</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .navbar-brand img {
            height: 60px; /* Adjust the height for logo size */
            width: 60px; /* Keep width consistent for a circle */
            border-radius: 50%; /* Makes the image circular */
            object-fit: cover; /* Ensures the image scales nicely */
        }

        .quote-btn2 {
            padding: 8px 20px;
            font-size: 1rem;
            font-weight: 600;
            border-radius: 8px;
            background-color: #0047AB;
            color: white;
            transition: all 0.3s ease;
            text-decoration: none;
        }

        .quote-btn2:hover {
            background-color: #003b93;
            transform: scale(1.05);
        }   


        nav {
            position: fixed;
            top: 0;
            width: 100%;
            
        }

    </style>
</head>
<body>
    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #052e6c; height: 80px; position: fixed; z-index: 1000;">
        <div class="container">
            <!--Logo and Brand Name-->
            <a class="navbar-brand d-flex align-items-center" href="/"> 
                <img src="/whiteLogo.png"  alt="Al's Windows logo" class="me-2" style="height: 50px; width: auto; object-fit: contain; border-radius: 0;">
            </a> 
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link <%= (page === 'home') ? 'active' : '' %>" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= (page === 'about') ? 'active' : '' %>" href="/aboutUs">About Us</a>
                    </li>
                    <!-- <li class="nav-item">
                        <a class="nav-link <%= (page === 'leads') ? 'active' : '' %>" href="/leads">Contact Us</a>
                    </li> -->
                    <li class="nav-item">
                        <a class="nav-link <%= (page === 'calculator') ? 'active' : '' %>" href="/calculator">Quote Calculator</a>
                    </li>          
                </ul>
            </div>
            <a class="btn-quote" id="navbarQuoteBtn">Get a Quote</a>
        </div>
    </nav>

    <!-- Add Bootstrap JS and Popper.js -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>
    <!-- <script>
        document.addEventListener('DOMContentLoaded', function () {
        const quoteBtn = document.getElementById('quoteBtn');
        const quoteFormWrapper = document.getElementById('quoteFormWrapper');
        const closeForm = document.getElementById('closeForm');

        quoteBtn?.addEventListener('click', () => {
            quoteFormWrapper?.classList.add('active');
        });

        closeForm?.addEventListener('click', () => {
            quoteFormWrapper?.classList.remove('active');
        });
        });
    </script> -->

</body>
</html>

<script>
    document.addEventListener('DOMContentLoaded', function () {
            const navbarBtn = document.getElementById('navbarQuoteBtn');
            const quoteFormWrapper = document.getElementById('quoteFormWrapper');
            const closeForm = document.getElementById('closeForm');

            navbarBtn?.addEventListener('click', () => {
            quoteFormWrapper?.classList.add('active');
            });

            sectionBtn?.addEventListener('click', () => {
            quoteFormWrapper?.classList.add('active');
            });

            closeForm?.addEventListener('click', () => {
            quoteFormWrapper?.classList.remove('active');
            });
        });
</script>


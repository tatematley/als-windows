<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Form Submission Success</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #ffffff;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            flex-direction: column;
            gap:0;
        }

        .success-container {
            text-align: center;
            padding: 20px;
            background-color: #ffffff;
            border: 1px solid #ddd;
            border-radius: 0px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            max-width: 500px;
            width: 100%;
            margin-top: 20px;
        }

        .success-icon {
            font-size: 50px;
            color: #003d80;
            margin-bottom: 20px;
        }

        .success-message {
            font-size: 1.5rem;
            color: #333;
            margin-bottom: 20px;
        }

        .success-details {
            font-size: 1rem;
            color: #666;
            margin-bottom: 30px;
        }

        .btn-home {
            display: inline-block;
            padding: 10px 20px;
            background-color: #003b93;
            color: #fff;
            font-size: 1rem;
            font-weight: bold;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .btn-home:hover {
            background-color: #003d80;
            transform: scale(1.05);
        }

        @media (max-width: 768px) {
            .success-container {
                padding: 20px;
            }
        }

        .banner-text {
            color: rgba(255, 255, 255, 0.9);
            font-size: 3rem;
            text-transform: uppercase;
            font-family: 'Oswald', sans-serif;
        }

        .logo {
            max-width: 450px; /* Restrict logo size */
            margin-bottom: -30px; /* Space below the logo */
        }
    </style>
</head>
<body>
    <%- include('partials/navbar') %>

    <img src="/images/smallLogo.jpeg" alt="Logo" class="logo">
    <br>
    <br>
    <!-- Success Message Section -->
    <div class="success-container">
        <div class="success-icon"><i class="fas fa-check-circle"></i></div>
        <div class="success-message">Thank you! Your form has been successfully submitted.</div>
        <div class="success-details">We will contact you soon</div>
        <button class="btn-home" onclick="window.location.href='/'">Go to Homepage</button>
    </div>
</body>
</html>